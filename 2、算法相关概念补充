
1、算法
	算法就是任何明确定义的计算过程，接受一些值或集合作为输入，并产生一些值或集合输出；就是将输入转换为输出的一系列计算过程；

	三个重要特性：
		必须是有限的
		必须具备明确定义的指令
		必须是有效的

2、对数
	log(10)100 相当于问"将多少个10相乘的结果为100" ，log(10)100=2;对数运算是幂运算的逆运算

	【定义】如果 N=ax（a>0,a≠1），即a的x次方等于N（a>0，且a≠1），那么数x叫做以a为底N的对数（logarithm），记作：
　　 x=logaN
　　其中，a叫做对数的底数，N叫做真数，x叫做 “以a为底N的对数”。

3、大O表示法
	是一种特殊的表示法，支出算法的速度有多快
	例如简单查找与二分查找的区别
	元素      	     简单查找 |	二分查找
	100个元素	     100ms	    7ms
	10000个元素	     10s	    14ms
	1000000000个元素	 11天	    30ms

	常见的大O运行时间
		o(log n),对数时间，包括二分算法
		o(n),线性时间，包括简单查找
		o(n*log n)快速排序
		o(n下标2)选择排序
		o(n!)阶乘时间

4、数据的逻辑机构的四种基本类型
	集合
	线性结构：数据元素存在一对一的关系
	树形结构：数据元素存在一对多的关系
	网状或图状结构：数据元素存在多对多的关系

5、数据结构的存储方式
	数据元素在计算机中有两种不同表示方法：顺序表示和非顺序表示，导出两种储存方式：
		顺序存储结构
		链式存储结构